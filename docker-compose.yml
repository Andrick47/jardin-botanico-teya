services:
  teya-web:
    build: .
    container_name: teya-web
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./media:/app/media
    ports:
      - "8000:8000"
    networks:
      - npm_proxy

networks:
  npm_proxy:
    external: true
    